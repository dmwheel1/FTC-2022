package org.firstinspires.ftc.teamcode.FusionFramework;

public class PositionVerifier {

    public boolean setPosition( int X, int Y ) {
        boolean returnValue = false; // default return indicates failure

        // Add implementation here

        return returnValue;
    }

    public boolean recalcPosition( int X, int Y ) {
        boolean returnValue = false; // default return indicates failure

        // Add implementation here

        return returnValue;
    }

    public boolean reportCurrPosition( int X, int Y ) {
        boolean returnValue = false; // default return indicates failure

        // Add implementation here

        return returnValue;
    }

    public boolean reportDistanceto( int X, int Y ) {
        boolean returnValue = false; // default return indicates failure

        // Add implementation here

        return returnValue;
    }

}
